#!/usr/bin/env bash
# Wrapper de la CLI. Fija FINANCE_ROOT a la raíz del proyecto.
set -euo pipefail
export FINANCE_ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
exec python3 "$FINANCE_ROOT/src/finance/cli.py" "$@"
